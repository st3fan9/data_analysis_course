name: <replace with a name for the conda environment>
channels:
  - conda-forge
dependencies:
  - python>3
  - jupyter
  - <replace here with name of the python library for tabular data analysis>
  - xlrd
